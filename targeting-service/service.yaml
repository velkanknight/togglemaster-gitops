# Endereço estável do targeting-service: http://targeting-service
apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: togglemaster
spec:
  type: ClusterIP
  selector:
    app: targeting-service
  ports:
    - port: 80
      targetPort: 8003
